{
  "name": "reliquary-companion",
  "private": true,
  "version": "0.2.1",
  "description": "Electron shell for the Artificer/Reliquary Companion (companion-cutover.md Phase 3): wraps the companiond daemon and web/companion's built renderer in a native window, tray, and OS integrations.",
  "author": "Artificer contributors",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && electron .",
    "test": "npm run build && node --test test/*.test.js",
    "build:daemon": "node scripts/build-daemon.js",
    "package": "npm run build && npm run build:daemon && electron-builder",
    "smoke": "npm run build && node scripts/smoke.js",
    "icon": "electron scripts/make-icon.js"
  },
  "dependencies": {
    "electron-updater": "^6.8.9"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "electron": "^32.2.0",
    "electron-builder": "^25.1.8",
    "typescript": "^5.5.3"
  }
}
